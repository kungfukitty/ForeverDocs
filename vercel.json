{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm ci || npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
